# 最大 CC 源点识别主计划

## 1. 任务核心

本任务的核心目标不是“能不能找到最大 CC”，而是：

**在最大 CC 内更准确地识别真实源点。**

现有实验已经说明两件事：

- 简单 2 层 GATv2 已经可以把注意力基本聚集到最大 CC 附近。
- 真正的主矛盾不是候选域定位，而是最大 CC 内部的精确溯源。

因此，后续所有实验都要围绕下面这条主线展开：

**最大 CC 内相对角色重打分 -> 源点个数估计 -> 图结构集合解码**


## 2. 当前证据与结论

### 2.1 已经成立的结论

- [main_gatv2_candidate_domain.py](E:/desk/diffuse/diff_true/SII/RTSD-base/main_gatv2_candidate_domain.py) 与 [main.py](E:/desk/diffuse/diff_true/SII/RTSD-base/main.py) 都表明，源点核心区域大概率在最大 CC 内。
- [最大cc实验结果分析.md](E:/desk/diffuse/diff_true/SII/RTSD-base/最大cc实验结果分析.md) 表明，在不少数据集上，`max_cc` 视角下的 `MAP / P@K_true / AED / Recall@K` 更优，说明最大 CC 已经是有效候选域。
- [phase0实验结果.md](E:/desk/diffuse/diff_true/SII/RTSD-base/phase0实验结果.md) 表明，简单 2 层 GATv2 已经是可靠基线，复杂骨干并不是当前主要瓶颈。

### 2.2 已经被实验否掉的方向

- `rank loss` 不是当前任务的有效主抓手，尤其是“只在最大 CC 内做 ranking”并没有带来提升，甚至会伤害 `MAP / P@K_true / AED / Recall@5`。
- 仅仅把监督范围切到最大 CC，并不能自动转化为“最大 CC 内更好的源点识别”。
- 后续不再把 `rank loss` 作为阶段 1 的主线设计。


## 3. 任务重定义

我们不再把问题理解为：

**全图上的独立节点分类。**

而是改写为：

**在全图编码的基础上，将最大 CC 作为候选域，在其中进行相对角色建模、源点个数估计和结构化集合恢复。**

这一定义的变化非常关键，因为它决定了后面的方法创新不应该停留在“换更强 GNN”或“加更多绝对统计量”上。


## 4. 理论假设

### 4.1 候选域集中假设

真实源点大概率集中在最大 CC 内。

这部分已经基本成立，因此后续不再把“证明最大 CC 是否重要”当成主任务。

### 4.2 角色可分性假设

在最大 CC 内，源点不是普通高热节点，而更像“中心型启动者”。

根据 [dataAnaly.md](E:/desk/diffuse/diff_true/SII/RTSD-base/dataAnaly.md) 的统计，源点通常具有以下角色画像：

- 更中心
- 更外向
- 度更高，但不是简单 hub
- `k_inf` 较高，但通常不是局部最强热点
- 会被更强邻居压制，但通常没有普通非源点那么严重

### 4.3 个数可变假设

不同级联的源点数不同，因此固定阈值不是最优解。

必须显式建模：

**当前级联在最大 CC 内应该选出多少个源点。**

### 4.4 集合一致性假设

源点最终应该被恢复为一个结构合理的集合，而不是一组独立高分点。

因此最终输出不能只依赖逐点分数，还需要集合级解码。


## 5. 总体方法主线

### 5.1 阶段 0：固定简单基线

目标：

- 固定简单 2 层 GATv2 作为统一骨干。
- 不再反复讨论“GATv2 能否学到最大 CC”。

结论用途：

- 作为后续所有实验的稳定对照底座。

当前共识：

- [main_gatv2_maxcc.py](E:/desk/diffuse/diff_true/SII/RTSD-base/main_gatv2_maxcc.py) 所代表的简单 GATv2 线可以作为后续主实验基线。


### 5.2 阶段 1：最大 CC 内相对角色重打分

目标：

- 把最大 CC 内的“热点节点”和“真正源点角色节点”区分开。

核心思想：

- 不再依赖 `rank loss`。
- 也不把重点放在继续增强全图点分类器上。
- 而是在最大 CC 内学习一个更贴近“源点角色”的局部重打分机制。

阶段 1 的本质是：

**源点角色打分，而不是节点 pairwise 排名。**

#### 阶段 1 的主特征方向

只保留真正贴近最大 CC 内溯源问题的相对角色特征：

- `中心性相对度残差`
- `中心性相对 kinf 残差`
- `中心性 x 外向性`
- `中心性 x 内核深度`
- `邻居压制平衡`

原则：

- 加“相对角色特征”
- 不加一堆新的绝对统计量
- 不把 exploratory 特征当主贡献

#### 阶段 1 的实现原则

- 骨干仍使用简单 2 层 GATv2
- 监督先保持稳定的 BCE 类目标
- 最终评测范围继续保持 `all_infected`
- 方法设计重点放在“最大 CC 内重打分”，不是改最终指标口径

#### 阶段 1 的成功标准

至少以下指标中的 2 到 3 项稳定提升：

- `MAP`
- `P@K_true`
- `Recall@5`
- `AED`


### 5.3 阶段 2：源点个数估计

目标：

- 明确预测当前级联在最大 CC 内应该有多少个源点。

这是整条创新路线里的第一个核心主创新。

建议做法：

- 在最大 CC 节点 embedding 上做 pooling
- 拼接少量图级统计量
- 单独增加 cardinality head
- 预测 `K_hat`

优先形式：

- `ordinal classification`

不建议一开始就做纯回归。

推荐使用的图级统计量：

- 最大 CC 大小
- 感染节点总数
- 最大 CC 占感染节点比例
- top score gap
- score entropy
- 局部高分峰数量

阶段 2 的成功标准：

- 源点个数 MAE 明显下降
- 下游 `F1 / P@K_true / AED` 比阶段 1 更好


### 5.4 阶段 3：图结构集合解码

目标：

- 从“最大 CC 内高分节点列表”解码出“合理的源点集合”。

这是整条创新路线里的第二个核心主创新。

阶段 3 的任务不是简单 `top-k`，而是：

**Count-aware Graph Set Decoding**

输入：

- 最大 CC 内角色分数 `s_i`
- 预测源点个数 `K_hat`
- 节点间图距离与局部结构信息

输出：

- 大小为 `K_hat` 的源点集合 `S`

第一版建议：

- `Soft Graph NMS + Greedy Set Selection`

不是硬 NMS，而是软抑制与贪心集合构造。

目标函数至少同时考虑：

- 节点分数高
- 集合内部不过度冗余
- 避免全部堆在同一个局部 hub 团
- 保持对多个潜在源区域的覆盖

阶段 3 的成功标准：

- `P@K_true` 提升
- `AED` 明显下降
- `Recall@K` 不出现明显掉点


## 6. 方法创新点

后续论文或方法总结，建议只凝练为以下 3 个创新点：

1. **最大 CC 约束下的相对角色重打分**
2. **源点个数估计**
3. **图结构集合解码**

不要把“加更多特征”当作主创新。
特征只是阶段 1 的支撑手段，不是整条方法的主体。


## 7. 实验推进顺序

严格按以下顺序推进：

1. 固定简单 GATv2 基线
2. 做最大 CC 内相对角色重打分
3. 做源点个数估计
4. 做图结构集合解码
5. 最后才考虑 joint tuning

每一步都必须先证明单独有效，再进入下一步。


## 8. 明确不做的事

- 不再把 `rank loss` 当作主线
- 不再把“max_cc vs all_infected 范围对比”当作主要研究问题
- 不继续把主要精力放在更复杂 encoder 上
- 不用“大量新特征堆叠”替代方法设计
- 不把多个阶段同时塞进一个大而全脚本里


## 9. 当前统一结论

当前最合适的研究路线已经明确收敛为：

**最大 CC 内相对角色重打分 -> 源点个数估计 -> 图结构集合解码**

这条线同时满足：

- 与当前实验事实一致
- 与 [最大cc实验结果分析.md](E:/desk/diffuse/diff_true/SII/RTSD-base/最大cc实验结果分析.md) 的结论一致
- 与 [dataAnaly.md](E:/desk/diffuse/diff_true/SII/RTSD-base/dataAnaly.md) 中的源点角色画像一致
- 具有后续继续做出创新方法的空间
